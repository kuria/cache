{
    "name": "kuria/cache",
    "description": "Caching library with several implementations (Filesystem, APC, XCache, Memache, Memory).",
    "keywords": ["cache", "caching", "APC", "XCache", "Memcache"],
    "license": "MIT",
    "require": {
        "php": ">=5.3.0"
    },
    "require": {
        "kuria/event": "~0.1.0"
    },
    "autoload": {
        "psr-4": {
            "Kuria\\Cache\\": "src"
        }
    }
}